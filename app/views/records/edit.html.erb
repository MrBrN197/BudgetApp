<h1>Editing record</h1>

<%= render "form", record: @record %>

<br>

<div>
  <%= link "Show this record", [@category, @record] %> |
  <%= link "Back to records", category_records_path(@category) %>
</div>
